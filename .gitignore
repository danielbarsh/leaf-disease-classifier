# Data - not committed to git (too large, and reproducible with prepare_data.py)
data/leaf_split/
data/_plantvillage_repo/
data/raw/

# Trained model - optional: you can remove this line if you want to commit
# it to git so the demo works right after clone (the model weighs ~9MB, not large)
# models/*.keras

# Standard Python files
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# PyTorch pipeline outputs (src/torch_pipeline) - reproducible via training,
# not committed. The Keras model/*.keras stays tracked (see above) for deployment.
models/*.pt
models/*.pth
models/*_torch.*
checkpoints/
runs/
wandb/
*.log

# OS / editor
.DS_Store
.vscode/
.idea/
